[package]
name = "sfo-pool"
version = "0.2.7"
edition = "2021"
license-file = "LICENSE"
repository = "https://github.com/wugren/sfo-pool.git"
description = "A work allocation pool"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
async-trait = "0.1"
notify-future = "0.2"
sfo-result = "0.2"
